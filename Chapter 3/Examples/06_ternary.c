#include<stdio.h>
    int main(){

    // Shorthand of if-else statement is ternary operator.
    // Syntax :- condition ? expression1 : expression2 ;
    // If condition is true then expression1 is executed otherwise expression2 is executed.    
    int a = 323;
    int b = 23324;
    a>b?printf("a is greater\n"):printf("b is greater\n");

    return 0;
}